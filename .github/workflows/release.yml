# Release artifacts on tag (s02 stub — proves the matrix mechanics; c13/s66
# add real cross-compilation, signing, and packaging).
name: release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  dist:
    name: dist (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        # tier-1 hosts: linux x86-64, linux aarch64, macOS aarch64, windows x86-64
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: build archive
        run: cargo xtask dist
      # The release body is the CHANGELOG's entry for this tag (#214): v0.2.1
      # and v0.2.2 both answered the releases API with a `body` of length 0,
      # because the step below created the release with `--title` and nothing
      # else. A tag with no CHANGELOG entry fails the job here, by name — an
      # empty body is exactly what this closes.
      - name: release notes from the CHANGELOG
        shell: bash
        run: cargo xtask release-notes "${GITHUB_REF_NAME}" --out target/release-notes.md
      # Every matrix job writes the same notes and passes them twice: on the
      # create, so whichever job wins the race sets the body, and on an edit,
      # so the body is right even for a release someone opened by hand.
      - name: upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          notes=target/release-notes.md
          gh release create "${GITHUB_REF_NAME}" --draft --title "${GITHUB_REF_NAME}" --notes-file "$notes" || echo "release already exists"
          gh release edit "${GITHUB_REF_NAME}" --notes-file "$notes"
          gh release upload "${GITHUB_REF_NAME}" target/dist/*.tar.gz --clobber

  # The last hand step, retired (#226). `gh release create --draft` above is
  # deliberate: a half-uploaded release page must never be public, and at
  # v0.2.0/v0.2.1 (#200) and again at v0.2.3 the draft outlived the matrix
  # because a human had to run `gh release edit --draft=false`. This job is
  # the other half of the pair. `needs: dist` is the whole matrix — with
  # `fail-fast: false` every host still runs, but this job runs ONLY if all
  # four succeeded, and is skipped the moment one is red. So a fully-green
  # matrix publishes itself and a red one stays a draft, which is the rule
  # the hand step was enforcing by memory.
  publish:
    name: publish (un-draft)
    needs: dist
    runs-on: ubuntu-latest
    steps:
      - name: publish the release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          # The evidence, in the log, before the flag moves: one archive per
          # matrix host. A green leg implies its upload (the glob would have
          # failed the step), so this is a tripwire on the glob rather than a
          # second gate on the hosts — and if it ever fires, the release stays
          # a draft, which is the safe side.
          gh release view "${GITHUB_REF_NAME}" --json assets \
            --jq ".assets[].name" | sort | tee assets.txt
          count=$(grep -c "\.tar\.gz$" assets.txt || true)
          if [ "$count" -lt 4 ]; then
            echo "publish: ${GITHUB_REF_NAME} carries $count archives, wanted 4 — leaving it a draft" >&2
            exit 1
          fi
          gh release edit "${GITHUB_REF_NAME}" --draft=false
          echo "publish: ${GITHUB_REF_NAME} is public with $count archives"
