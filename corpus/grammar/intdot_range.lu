//! check: pass
//! phase: run
//! conforms: gram.amb.intdot
//! `1..10` is a range — two dots never lex as a float.

fn main() -> !int {
    var sum = 0
    for i in 1..10 { sum += i }    // 45
    for i in 1..=10 { sum += i }   // +55
    if sum == 100 { 0 } else { 1 }
}
