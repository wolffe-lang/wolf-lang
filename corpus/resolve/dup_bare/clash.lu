//! check: fail(E0302)
//! phase: parse
//! conforms: conf.directive.standalone, mod.dup
//!
//! #149 probe 2: the duplicate lives in a bare sibling. `other.lu`
//! (no directives, so a member by D59) defines `helper` again, and
//! wolf reports E0302 with both sites — the same answer lupin gave
//! while `wolf build` was still ignoring siblings.

fn helper() -> int {
    1
}

fn main() -> !int {
    helper()
}
