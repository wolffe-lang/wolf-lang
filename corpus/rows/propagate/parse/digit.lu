//! member: true
//! conforms: err.rows
//!
//! Sibling file of the same module (directory = module): a private
//! helper with an explicit single-tag row.

fn digit(n: int) -> int ! {bad_digit} {
    if n < 0 { return bad_digit }
    if n > 9 { return bad_digit }
    n
}
