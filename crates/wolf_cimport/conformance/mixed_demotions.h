/* The property the demotion ladder exists for: one cursed declaration
   never costs its siblings anything. Every neighbour here stays
   usable. */
int good_before(int x);
long double cursed_middle(long double x);
union tagless { int i; float f; };
int ancient_style();
int good_after(int x);
