---
source: crates/wolf_sema/tests/wave_diagnostics.rs
expression: "render_warnings(src, true)"
---
warning[W0601]: this `() ! {cancelled, closed}` result is discarded, error row and all
 --> mem:///main.lu:2:5
  |
2 |     ch.send(0)
  |     ^^^^^^^^^^ the block's value is `()`, so a failure here vanishes silently
  |
  = note: propagate it with `?`, handle it with `else`, or bind it away explicitly so the discard
    is visible.

warning[W0601]: this `() ! {cancelled, closed}` result is discarded, error row and all
 --> mem:///main.lu:6:22
  |
6 |     for i in 1..=2 { ch.send(i) }
  |                      ^^^^^^^^^^ the block's value is `()`, so a failure here vanishes silently
  |
  = note: propagate it with `?`, handle it with `else`, or bind it away explicitly so the discard
    is visible.

warning[W0601]: this `() ! {cancelled, closed}` result is discarded, error row and all
 --> mem:///main.lu:7:15
  |
7 |     if true { ch.send(3) }
  |               ^^^^^^^^^^ the block's value is `()`, so a failure here vanishes silently
  |
  = note: propagate it with `?`, handle it with `else`, or bind it away explicitly so the discard
    is visible.
