//! member: true
//!
//! The `shapes` module: one `pub` export and one private helper that
//! uses it — a cross-file reference in both directions.

/// The square's area; total for any `side`.
pub fn area(side: int) -> int {
    side * side
}

fn double(side: int) -> int {
    area(side) + side
}
