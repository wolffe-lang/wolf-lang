[package]
name = "wolf_wir"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
# s24 crate hygiene: wolf_wir depends only on wolf_span. Sema/mem types
# do NOT leak in — the fact vocabulary is re-declared here as IR-level
# enums (facts.rs documents the mapping from c04's theorem classes).
# wolf_diag joins in s25 when the driver wraps verifier failures as ICEs.
wolf_span.workspace = true

[dev-dependencies]
insta.workspace = true
