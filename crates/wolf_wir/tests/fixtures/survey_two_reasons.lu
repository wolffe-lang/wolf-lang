//! The survey lens's own witness (NOT a corpus file — never in the
//! ledger): one function, two INDEPENDENT refusal-provoking
//! statements. Fail-fast refuses at the first and masks the second;
//! the survey collects both, in source order, with a clean statement
//! lowered between them to prove the lens continued.
//!
//! The refusing construct is a module-STATE read (`var` item — a
//! `let` would constant-fold at the use site): the c06 residue s95
//! left named, after fn-value reads started lowering (the fixture's
//! previous construct — updated with the sprint).

var base: int = 1

fn main() -> !int {
    let a = base
    print("between")
    let b = base
    if a + b == 0 { 1 } else { 0 }
}
