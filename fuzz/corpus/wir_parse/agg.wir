; small aggregates by value list: built and taken apart by field.
fn @pair(i64, f64) -> {i64, f64} {
b0(%a: i64, %b: f64):
  %p = agg.make %a, %b
  ret %p
}

fn @first(i64, f64) -> i64 {
b0(%a: i64, %b: f64):
  %p = agg.make %a, %b
  %x = agg.get %p, 0
  ret %x
}
